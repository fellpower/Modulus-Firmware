# Firmware build outputs
firmware/**/build/
firmware/**/build_ota*/
firmware/**/sdkconfig
firmware/**/sdkconfig.old
firmware/**/sdkconfig.ota*
firmware/**/managed_components/

# NOT dependencies.lock — it is tracked on purpose.
#
# Ignoring it meant every clone re-solved the component graph against the live
# registry instead of the versions we build with, so the repo was unbuildable
# by anyone but the machine that had a stale local solve. That is the root
# cause of BufferRoot/Modulus-Firmware#2: esp_lvgl_port drifted to 2.9.0
# (needs on_frame_buf_complete, absent in IDF 6.0.1), esp-audio-player 1.0.7
# broke on GCC 15, and esp_wifi_remote resolved differently.
# managed_components/ stays ignored — it is the cache the lock reproduces.

# Vendor reference clone
vendor/

# Zig
.zig-cache/
zig-out/
zig-pkg/

# Packaged flash images (upload via GitHub Releases — do not commit binaries)
dist/

# Node / local tooling (not required to build/flash)
node_modules/
**/node_modules/
package.json
**/package.json
package-lock.json
**/package-lock.json
.fallowrc.json
.tmp_*
p.status.wcs
{s}
*.err
build-audit-*

# Agent / IDE / docs (not required to use firmware)
.agents/
.cursor/
.claude/
.claude-flow/
.evolver/
.fallow/
.swarm/
.token-savior-cache.json
ruvector.db
memory/
docs/
CLAUDE.md
MEMORY.md
.contextignore
.zls.json
.env.example

# Dev-only scripts (regenerate / soak / MCP)
scripts/__pycache__/
scripts/audit_*.ps1
scripts/gen_phosphor_icons.mjs
scripts/m3_ux_*.py
scripts/monitor_*.py
scripts/setup_*.ps1
scripts/soak_*.ps1
scripts/split_*.py
scripts/tmp_*.bat
tools/mmbp_bridge/
tools/gen_zb_devdb.py

# Env / secrets
.env
.env.*

# Logs / capture dumps
*.log
*.log.err
.cold_boot*.log
monitor*.log
flash-*.log
build-*.log
build-*.err
boot_capture*.txt
build_*.txt
build-*.txt
test_*.txt
test-*.txt
cap_touch.txt
tab5_bridge_build.txt
_ps_out.txt
_zig_env.txt
logs/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
